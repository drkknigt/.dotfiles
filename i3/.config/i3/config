# set mod key
set $mod Mod1

# exec --no-startup-id picom -CGB
# Font for window titles. Will also be used by the bar unless a different font

#font  used (is used in the bar {} block below.)
font pango:FiraCode 11

# excecute feh to set background
exec_always --no-startup-id ~/.fehbg

# execute polybar
exec_always --no-startup-id killall polybar; polybar

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet
exec --no-startup-id /usr/libexec/gnome-settings-daemaon-localeexec

#exec --no-startup-id /home/devops/.config/i3/xrnd/send2primary.sh
# monitor settings

# autolock after 45 mins
exec_always xautolock -time 45 -locker "i3lock -c 000000 && xset dpms force standby"
exec_always xset dpms 0 0 7220

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status

# assign extra keys on keyboard for volume control
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# binding to increase/decrease the brightness levels of the system
bindsym $mod+"plus" exec --no-startup-id brightnessctl set +3%
bindsym $mod+"minus" exec --no-startup-id brightnessctl set 3%-

# monitor settings
workspace 1 output eDP-1
workspace 2 output DP-1-3
exec_always --no-startup-id xrandr --output DP-1-3 --right-of eDP-1
exec_always --no-startup-id xrandr --output DP-1-3 --gamma 0.9:0.9:0.9
exec_always --no-startup-id xrandr --output eDP-1 --mode 1366x768  --gamma 0.7:0.7:0.7
#exec_always --no-startup-id compton --vsync opengl-swc --backend glx

# picom compositor settings
exec_always --no-startup-id picom 

# battery down notifcation
exec_always --no-startup-id killall check_battery.sh;/home/drkknght/bin/system_scripts/check_battery.sh

# set the cursor speed
exec_always --no-startup-id xset r rate 250 100

# swap control and caps lock keys
exec_always --no-startup-id setxkbmap -option "ctrl:swapcaps"

# start alacritty on the startup
exec_always --no-startup-id alacritty --class "startup"

# exec_always ~/.fehbg &

# set workspace to appropriate screens
workspace 1 output eDP-1
workspace 2 output DP-1-3

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal with mod + enter
bindsym $mod+Return exec --no-startup-id alacritty -t "startup" 

# start a color chooser
bindsym $mod+control+c exec --no-startup-id kcolorchooser

# kill focused window
bindsym $mod+q kill

# bindsym $mod+Control+a exec i3-sensible-terminal -e 'xdg-open $(fzf)'

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run 

# run terminal programs 
bindsym $mod+t exec --no-startup-id rofi -show run 

# bindsym $mod+Control+d exec --no-startup-id i3-sensible-terminal -e "echo '$(pwd)\n' > ~/new.txt"

# run graphical programs
bindsym $mod+g exec --no-startup-id rofi -show drun -show-icons

#bindsym $mod+Shift+e exec --no-startup-id  /home/drkknght/bin/navigation/search_dir.sh
#bindsym $mod+Shift+f exec --no-startup-id /home/drkknght/bin/navigation/search.sh

# show all i3 windows
bindsym $mod+Control+w exec --no-startup-id rofi -show

# start vlc media player
bindsym $mod+Control+v exec --no-startup-id 'vlc'

# start browser
bindsym $mod+b exec --no-startup-id 'brave-browser'

# start telegram-desktop
bindsym $mod+s exec --no-startup-id 'telegram-desktop'

# open file manager
bindsym $mod+o exec --no-startup-id 'nemo'

# set background for monitor 1
bindsym $mod+"bracketleft" exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/background/change_background.sh 0


# open qcal
bindsym $mod+c exec --no-startup-id alacritty -t "cal" -o window.opacity=0.8 -o font.size=13 -e qalc


# set background for monitor 2
bindsym $mod+"bracketright" exec  --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/background/change_background.sh 1

# fuzzy find the file and open it
bindsym $mod+f exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/system_scripts/file_exp.sh f

# see all the keymaps for i3wm
bindsym $mod+z exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/system_scripts/keymap.sh

# fuzzy find the directory and open it
bindsym $mod+d exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/system_scripts/file_exp.sh d

# kill any process
bindsym $mod+x exec --no-startup-id alacritty -o window.opacity=0.9 -t "fzfmenu" -e htop

# open man pages
bindsym $mod+"slash" exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/system_scripts/man_pages.sh

# bindsym $mod+Shift+n exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/network/wifi_fzf2.sh

# mount devices
bindsym $mod+m exec --no-startup-id alacritty -t "mount" -e /home/drkknght/bin/system_scripts/mount.sh

bindsym $mod+n exec --no-startup-id alacritty -t "fzfmenu" -e /home/drkknght/bin/network/wifi_fzf.sh
bindsym -x '"\C-o": file="$(fzf --height 40% --reverse)" && [ -f "$file" ] && xdg-open "$file"'
# bindsym $mod+g exec  /home/drkknght/bin/tmux_scripts/brightness.sh 5
# bindsym $mod+- exec  /home/drkknght/bin/tmux_scripts/brightness.sh -5

# connect and disconnect wifi
bindsym $mod+i exec /home/drkknght/bin/system_scripts/wifi_connect.sh

# bindsym $mod+Control+d exec /home/drkknght/bin/fd.sh
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Control+h move left
bindsym $mod+Control+k move up
bindsym $mod+Control+j move down
bindsym $mod+Control+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+u split h

# swithc off the computer!
bindsym $mod+Control+e exec  systemctl poweroff

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+a fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+y layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Control+f floating toggle
bindsym $mod+Shift+t exec "i3lock -c 000000"

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+Control+i focus parent

# focus the child container
#bindsym $mod+Control+y focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
# set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Control+1 move container to workspace number $ws1 
bindsym $mod+Control+2 move container to workspace number $ws2
bindsym $mod+Control+3 move container to workspace number $ws3
bindsym $mod+Control+4 move container to workspace number $ws4
bindsym $mod+Control+5 move container to workspace number $ws5
bindsym $mod+Control+6 move container to workspace number $ws6
bindsym $mod+Control+7 move container to workspace number $ws7
bindsym $mod+Control+8 move container to workspace number $ws8
bindsym $mod+Control+9 move container to workspace number $ws9
bindsym $mod+Control+0 move container to workspace number $ws10

# reload the configuration file

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+r restart

bindsym $mod+Control+r systemctl reboot

# exit i3 (logs you out of your X session)
# bindsym $mod+Control+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Control+q exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        #bindsym $mod+Control+r mode "default"
}

#bindsym $mod+Control+r mode "resize"
# Moving workpspaces between screens
bindsym $mod+p move workspace to output right

# Resizing windows in i3 using keyboard only
# https://unix.stackexchange.com/q/255344/150597

# Resizing by 1
bindsym $mod+Ctrl+Right resize shrink width 1 px or 1 ppt
bindsym $mod+Ctrl+Up resize grow height 1 px or 1 ppt
bindsym $mod+Ctrl+Down resize shrink height 1 px or 1 ppt
bindsym $mod+Ctrl+Left resize grow width 1 px or 1 ppt

# Resizing by 10
bindsym $mod+Ctrl+Shift+Right resize shrink width 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Up resize grow height 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Down resize shrink height 10 px or 10 ppt
bindsym $mod+Ctrl+Shift+Left resize grow width 10 px or 10 ppt

# focus_on_window_activation focus
# workspace_auto_back_and_forth yes
bindsym $mod+"period" workspace back_and_forth

# bindsym $mod+Shift+b move container to workspace back_and_forth; workspace back_and_forth
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        tray_output primary
#        output primary
#        # status_command i3status -c ~/.config/i3status/i3status.conf
#        separator_symbol "❰"
#        colors{
#                background #303e2e
#                statusline #ffffff
#                separator #000000
#            }
#
#}
client.focused          #000000   #305b5c #ffffff
client.focused_inactive #333333 #444547 #8a8787
client.unfocused        #333333 #454343 #888888
client.urgent           #2f343a #900000 #ffffff
client.background #000000

# assign program to workspaces
for_window [floating] move position center 
for_window [class="Nemo"] move to workspace $ws3
for_window [class="Brave-browser"] move to workspace $ws2
for_window [class="Zettlr"] move to workspace $ws6
for_window [class="Joplin"] move to workspace $ws6
for_window [class="Code"] move to workspace $ws7
for_window [instance="startup"] move to workspace $ws1
for_window [title="startup"] move to workspace $ws1
for_window [class="vlc"] move to workspace $ws4
for_window [class="Zathura"] move to workspace $ws5
for_window [title="fzfmenu"] floating enable
for_window [title="mount"] floating enable
for_window [title="mount"] resize set 500 400
for_window [title="fzfmenu"] resize set 1200 500
for_window [title="cal"] floating enable
for_window [title="cal"] resize set 500 200
for_window [class="Gnome-terminal"] floating enable 
for_window [class="^.*"] focus


for_window [class="^.*"] border pixel 4
gaps inner 8
gaps outer 8


# scratchpad
bindsym $mod+Control+s move scratchpad
bindsym $mod+Control+d scratchpad show
